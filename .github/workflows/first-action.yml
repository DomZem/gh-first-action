# name of the workflow
name: First Workflow
# when run our workflow
on: workflow_dispatch
jobs: 
  # job name
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done - bye!"
    
